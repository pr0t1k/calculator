import React from "react";
import App from "../../App";

export default {
    title: "calc",
    component: App,
}

const Template = (args) => <App {...args} />;

export const Screen = Template.bind({});